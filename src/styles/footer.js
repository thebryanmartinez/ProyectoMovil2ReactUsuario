import { StyleSheet } from "react-native";

export const globalFooter = StyleSheet.create({
  footer: {
    display: "flex",
    backgroundColor: "#ed7731",
    padding: 10,
    flexDirection: "row",
    justifyContent: "space-around",
  },
});
